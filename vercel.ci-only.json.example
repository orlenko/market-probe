{
  "buildCommand": "npm run type-check && npm run lint && npm run test:ci && npm run build",
  "installCommand": "npm ci",
  "framework": "nextjs",
  "functions": {
    "src/app/api/**": {
      "maxDuration": 30
    }
  }
}
